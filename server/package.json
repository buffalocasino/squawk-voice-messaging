{
  "name": "squawk-signaling-server",
  "version": "0.0.1",
  "type": "module",
  "description": "Squawk P2P WebSocket signaling server — key bundle relay + peer presence",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": { "node": ">=18" }
}